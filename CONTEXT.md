# CONTEXT.md — 术语表

> 本文件只记录领域术语，不含实现细节。新增/变更术语时同步更新。

## 核心概念

- **许可证验证 (license verification)**：程序校验凭证有效性的逻辑与流程。统一术语，避免与"破解/绕过"混用。
- **凭证 (credential)**：证明许可权利的载体，如 license 文件、注册码、序列号、激活响应。
- **注册机 (keygen)**：通过伪造凭证或替换密钥材料，使未授权用户获得有效许可的可执行工具。
- **攻击原语 (attack primitive)**：针对验证机制的干预手段。三分：
  - **密钥替换 (key substitution)**：将自定义公钥写入目标文件，使自有私钥签发的凭证通过验证。
  - **密钥提取 (key extraction)**：从目标文件中取出内嵌的私钥或对称密钥，直接用于伪造凭证。
  - **凭证伪造 (credential forgery)**：不修改目标文件，按验证算法重实现或重演凭证生成。

  > 注：`doc/analysis.md` 与 README 中的"方案/路径"（version.dll 劫持、EXE 补丁、RSA 注册机）
  > 对应本表原语及"补丁/劫持"概念；"RSA 注册机"= 密钥替换。
- **补丁 (patch)**：修改目标文件的验证逻辑（如条件跳转改写、函数入口改写）。**不属于注册机**。
- **劫持 (hijack)**：不改目标文件，通过代理 DLL 在加载期干预。**不属于注册机**。

## 验证谱系 (verification lineage)

许可证验证机制的分类维度，决定注册机可行性：

| 谱系 | 注册机可行性 | 对应攻击原语 |
|------|-------------|-------------|
| 非对称签名 (RSA/ECDSA/DSA/Ed25519) + 内嵌公钥 | 可行 | 密钥替换 |
| 对称认证 (HMAC/AES-CMAC 等) + 内嵌密钥 | 可行 | 密钥提取 |
| 纯算法序列号 (name+serial 哈希类) | 可行 | 凭证伪造 |
| 在线激活（服务器验签） | 不可行 | 无；只能转补丁/劫持 |
| 壳保护（VMProtect/Themida 等） | 需先脱壳 | 前置步骤，非注册机 |

## 分析流程术语

- **锚点 (anchor)**：语义稳定的定位依据，如许可证错误字符串、固定密钥格式模板。
- **签名消息 (signing message)**：验证方对被签名数据的具体字节构造（如字段按序 `\x00` 连接）。
- **密钥材料 (key material)**：验证方用于校验凭证的密钥数据（公钥 DER、对称密钥、算法常量）。
